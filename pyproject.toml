[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "code-index"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
code_index = "code_index.__main__:main"

[tool.setuptools.packages.find]
include = ["code_index*"]
